class Post {
  int id;
  String title;
  String body;
  DateTime created_at;
  DateTime updated_at;

  Post({
    this.id,
    this.title,
    this.body,
    this.created_at,
    this.updated_at
  });
}